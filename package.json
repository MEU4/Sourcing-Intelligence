{
    "name": "sourcing-intelligence-monorepo",
    "private": true,
    "scripts": {
        "install": "cd api && npm install && cd ../frontend && npm install",
        "build": "cd frontend && npm run build"
    }
}